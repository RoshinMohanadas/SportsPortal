<%= form_with(model: newsfile, local: true) do |form| %>
  <% if newsfile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(newsfile.errors.count, "error") %> prohibited this newsfile from being saved:</h2>

      <ul>
        <% newsfile.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title,class: 'form-control' %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description,class: 'form-control' %>
  </div>

  <div class="actions">
  <%= form.submit class: "btn btn-primary form-control" %>
  </div>
<% end %>
