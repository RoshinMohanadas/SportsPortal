<%= form_with(model: event, local: true) do |form| %>
  <% if event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
        <% event.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name,class: 'form-control' %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description,class: 'form-control' %>
  </div>

  <div class="field">
    <%= form.label :eventtype %>
    <%= form.text_field :eventtype,class: 'form-control' %>
  </div>

  <div class="field">
    <%= form.label :rulesfile %>
    <%= form.file_field :rulesfile,class: 'form-control' %>
  </div>

  <div class="field">
    <%= form.label :eventgroup_id %>
    <%= form.collection_select :eventgroup_id, eventgroups.order(:name), :id, :name, {prompt: 'Select Event Group'}, class: 'form-control'%>
  </div> 

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
